The high-level processing steps are 1. detect features on RGB images, 2. track them with optical flow, 3. project them from RGB pixel frame to event pixel frame, 4. save pixel positions and event data
# Steps
1. Convert 